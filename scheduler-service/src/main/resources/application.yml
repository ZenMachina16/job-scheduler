spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobscheduler
    username: admin
    password: secret
  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true